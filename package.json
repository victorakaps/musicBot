{
  "name": "victormusicdownloaderbot",
  "version": "1.0.0",
  "description": "downloads songs",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "env-cmd -f ./dev.env node bot.js"
  }
}
